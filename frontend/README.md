# SaaS 产品分析前端

基于 Next.js 14 + Tailwind CSS 的 SaaS 产品分析展示平台。

## 技术栈

- **框架**: Next.js 14 (App Router)
- **样式**: Tailwind CSS
- **图标**: FontAwesome
- **图表**: Recharts
- **语言**: TypeScript

## 功能模块

### 📊 数据概览
- 关键指标统计（产品总数、蓝海赛道、机会产品等）
- 机会产品 TOP 榜单
- 市场类型分布
- 赛道分析概览

### 📈 赛道分析
- 全部赛道列表与筛选
- 市场类型分类（蓝海/红海/新兴/集中/适中/弱需求）
- 赛道详情面板（雷达图、详细指标）
- 多维度排序

### 📦 产品库
- 全部产品列表（分页、搜索、筛选）
- 机会产品专属视图
- 产品卡片展示

### 🔍 产品详情
- 综合评分雷达图
- 六维评分详情
- 选品分析（产品特征、组合匹配）
- 综合建议（优势/风险/建议）
- Landing Page 分析详情
- 数据完整度展示

## 快速开始

### 1. 安装依赖

```bash
cd frontend
npm install
```

### 2. 启动开发服务器

```bash
npm run dev
```

前端默认运行在 http://localhost:3000

### 3. 确保后端服务运行

前端需要后端 API 支持，请确保后端服务运行在 http://localhost:8001

```bash
cd backend
python -m uvicorn api.main:app --reload --port 8001
```

## 项目结构

```
frontend/
├── src/
│   ├── app/                    # Next.js App Router 页面
│   │   ├── page.tsx            # 首页（数据概览）
│   │   ├── layout.tsx          # 根布局
│   │   ├── globals.css         # 全局样式
│   │   ├── categories/         # 赛道分析页
│   │   ├── products/           # 产品列表页
│   │   │   └── [slug]/         # 产品详情页
│   │   └── about/              # 分析说明页
│   ├── components/
│   │   ├── layout/             # 布局组件（Sidebar, Header）
│   │   ├── dashboard/          # 仪表盘组件
│   │   ├── charts/             # 图表组件（RadarChart, ScoreBar）
│   │   ├── products/           # 产品相关组件
│   │   └── ui/                 # 通用 UI 组件（Card, Badge, Loading）
│   ├── lib/
│   │   ├── api.ts              # API 请求封装
│   │   └── utils.ts            # 工具函数
│   └── types/
│       └── index.ts            # TypeScript 类型定义
├── package.json
├── tailwind.config.ts          # Tailwind 配置（设计系统）
├── tsconfig.json
└── next.config.js              # Next.js 配置（API 代理）
```

## 设计系统

### 颜色

| 类型 | 颜色 | 用途 |
|-----|------|------|
| 蓝海市场 | `#06b6d4` | 低竞争高收益 |
| 红海市场 | `#f43f5e` | 竞争激烈 |
| 新兴市场 | `#f59e0b` | 早期机会 |
| 适中市场 | `#64748b` | 需差异化 |
| 集中市场 | `#a855f7` | 头部集中 |
| 弱需求 | `#475569` | 建议避开 |

### 评分颜色

- 7-10 分：绿色（优秀）
- 5-7 分：黄色（中等）
- 0-5 分：红色（较差）

## API 端点

前端通过 Next.js rewrites 代理到后端：

- `GET /api/startups` - 产品列表
- `GET /api/startups/:slug` - 产品详情
- `GET /api/analysis/category/` - 赛道分析
- `GET /api/analysis/product/opportunities` - 机会产品
- `GET /api/analysis/product/:slug` - 选品分析
- `GET /api/analysis/landing/:slug` - Landing Page 分析
- `GET /api/analysis/product/comprehensive/:slug` - 综合分析

## 构建部署

```bash
# 构建生产版本
npm run build

# 启动生产服务器
npm start
```

## 注意事项

1. 确保后端 API 服务运行正常
2. 部分页面需要先运行分析才有数据
3. 图表数据依赖后端分析结果
