# 发现页 MVP 开发计划

## 一、当前状态

### 1.1 后端（已完成）
- ✅ 母题判断：814 个产品已判断
- ✅ 专题生成：10 个专题已生成
- ✅ 数据存储：`mother_theme_judgments` 表、JSON 文件

### 1.2 前端（已有骨架）
- ✅ 页面结构：`/discover` 路由已存在
- ✅ 组件骨架：TodayCuration、TopicCollections 等
- ❌ 数据对接：目前使用 mock 数据

---

## 二、MVP 目标

### 核心功能
1. **主题合集列表** - 展示 10 个 AI 生成的专题
2. **专题详情页** - 展示专题内的产品列表
3. **产品卡片** - 展示产品基本信息 + AI 判断标签

### 不包含（后续迭代）
- 今日 AI 策展（需要更复杂的内容生成）
- 爆款解剖
- 创作者宇宙
- 个性化推荐

---

## 三、技术方案

### 3.1 后端 API

#### API 1: 获取专题列表
```
GET /api/discover/topics

Response:
{
  "topics": [
    {
      "id": "cautious_indie_dev_action_0",
      "title": "最适合独立开发者的产品方向",
      "description": "这些产品一个人就能做...",
      "curator_role": "谨慎的独立开发者",
      "pattern": "action",
      "product_count": 167,
      "emoji": "🧑‍💻"
    },
    ...
  ]
}
```

#### API 2: 获取专题详情
```
GET /api/discover/topics/{topic_id}?page=1&limit=20

Response:
{
  "topic": {
    "id": "...",
    "title": "...",
    "description": "...",
    ...
  },
  "products": [
    {
      "id": 123,
      "name": "Rezi",
      "slug": "rezi",
      "description": "...",
      "revenue_30d": 15000,
      "category": "AI",
      "website_url": "...",
      "judgments": {
        "solo_feasibility": "非常适合",
        "entry_barrier": "低门槛快启动",
        ...
      }
    },
    ...
  ],
  "total": 167,
  "page": 1,
  "limit": 20
}
```

### 3.2 前端改动

#### 文件清单
```
frontend/src/
├── app/(dashboard)/discover/
│   ├── page.tsx                    # 修改：对接 API
│   ├── topics/[id]/page.tsx        # 新增：专题详情页
│   └── components/
│       ├── TopicCollections.tsx    # 修改：对接 API
│       └── TopicCard.tsx           # 新增：专题卡片组件
├── lib/
│   └── api/discover.ts             # 新增：API 调用函数
└── types/
    └── discover.ts                 # 新增：类型定义
```

---

## 四、实现步骤

### Phase 1: 后端 API（0.5 天）

#### 任务 1.1: 创建 discover 路由
- [ ] 新建 `backend/api/routes/discover.py`
- [ ] 实现 `GET /api/discover/topics` 接口
- [ ] 实现 `GET /api/discover/topics/{topic_id}` 接口

#### 任务 1.2: 数据查询优化
- [ ] 从 JSON 文件读取专题数据（MVP 阶段）
- [ ] 关联查询产品详情
- [ ] 添加分页支持

### Phase 2: 前端对接（1 天）

#### 任务 2.1: API 层
- [ ] 创建 `lib/api/discover.ts`
- [ ] 定义 TypeScript 类型

#### 任务 2.2: 主题合集组件
- [ ] 修改 `TopicCollections.tsx` 对接真实 API
- [ ] 添加 loading 状态
- [ ] 添加错误处理

#### 任务 2.3: 专题详情页
- [ ] 创建 `topics/[id]/page.tsx`
- [ ] 产品列表展示
- [ ] 分页加载

### Phase 3: 样式优化（0.5 天）

#### 任务 3.1: 专题卡片
- [ ] 根据 pattern 显示不同颜色/图标
- [ ] 产品数量展示

#### 任务 3.2: 产品卡片
- [ ] 展示 AI 判断标签
- [ ] 收入数据格式化
- [ ] 跳转到产品详情

---

## 五、数据映射

### 专题 emoji 映射
```typescript
const TOPIC_EMOJI: Record<string, string> = {
  'cautious_indie_dev': '🧑‍💻',
  'quick_starter': '🚀',
  'opportunity_hunter': '🎯',
  'anti_bubble': '⚠️',
  'product_driven_fan': '💎',
  'niche_hunter': '🔍',
  'experience_diff': '✨',
  'low_risk_starter': '🛡️',
  'content_creator': '📝',
  'scene_focused': '🎬',
}
```

### 专题颜色映射
```typescript
const TOPIC_COLORS: Record<string, string> = {
  'action': 'from-blue-500 to-cyan-500',
  'niche': 'from-violet-500 to-purple-500',
  'cognitive': 'from-amber-500 to-orange-500',
  'contrast': 'from-emerald-500 to-green-500',
}
```

---

## 六、预估工时

| 阶段 | 任务 | 工时 |
|------|------|------|
| Phase 1 | 后端 API | 0.5 天 |
| Phase 2 | 前端对接 | 1 天 |
| Phase 3 | 样式优化 | 0.5 天 |
| **总计** | | **2 天** |

---

## 七、后续迭代

### v1.1: 今日 AI 策展
- 每日自动选择 1-2 个专题重点推荐
- AI 生成策展文案

### v1.2: 专题详情增强
- 产品对比功能
- "问 AI" 入口
- 收藏功能

### v1.3: 个性化推荐
- 基于用户浏览历史
- 基于用户偏好设置
